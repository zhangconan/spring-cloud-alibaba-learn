server:
  port: 8862

spring:
  application:
    name: cloud-alibaba-zipkin-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8861/eureka/
      
management:
  metrics:
    web:
      server:
        auto-time-requests: false