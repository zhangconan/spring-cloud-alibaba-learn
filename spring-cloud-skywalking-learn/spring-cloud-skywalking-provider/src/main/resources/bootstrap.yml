server:
  port: 8726

spring:
  application:
    name: spring-cloud-skywalking-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

logging:
  level:
    org.springframework: debug
